# CFB Roster Portal — copy to .env and fill in. (.env is gitignored.)

# Required ONLY for the data-collection scripts (scripts/collect-*).
# Used server-side (Node) only; NOT VITE_-prefixed, so it never reaches the client bundle.
CFBD_API_KEY=your_cfbd_api_key_here
CFBD_SEASON=2025
